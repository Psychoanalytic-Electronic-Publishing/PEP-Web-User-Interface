<li>
    <CheckboxInput
        @checked={{this.isChecked}}
        @onChange={{fn @onFacetChange @facetId @option.id}}
    >
        <span class="text-break">{{@option.label}}</span>
        <span class="text-muted small">
            {{t "search.refine.option.numResults" count=(format-number @option.numResults)}}
        </span>
    </CheckboxInput>
</li>
