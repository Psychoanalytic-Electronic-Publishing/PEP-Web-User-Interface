<ModalDialog @onClose={{@onClose}} @size="md" as |Modal|>
    <Modal.header @title={{t "preferences.title"}} />
    <Modal.body>
        <p class="text-muted">
            {{t "preferences.info"}}
        </p>
        <div class="mb-4">
            <h6 class="mb-3">{{t "preferences.general.heading"}}</h6>
            <div class="form-row">
                <div class="col-sm-7">
                    <FlSelect
                        @options={{this.theme.themeOptions}}
                        @value={{this.theme.currentTheme.id}}
                        @valuePath="id"
                        @labelPath="label"
                        @placeholder={{t "preferences.general.theme.label"}}
                        @emptyOptionText={{t "preferences.general.theme.select"}}
                        @allowClear={{false}}
                        @onChange={{this.updateTheme}}
                    />
                </div>
            </div>
        </div>
        <div class="mb-4">
            <h6 class="mb-3">{{t "preferences.search.heading"}}</h6>
            <div class="form-row">
                <div class="col-sm-7">
                    <FlSelect
                        @options={{this.searchPreviewOptions}}
                        @value={{this.currentUser.preferences.searchPreviewEnabled}}
                        @valuePath="value"
                        @labelPath="label"
                        @placeholder={{t "preferences.search.preview.label"}}
                        @emptyOptionText={{t "preferences.search.preview.select"}}
                        @allowClear={{false}}
                        @onChange={{fn this.updatePreference this.searchEnabledKey true}}
                    />
                </div>
            </div>
        </div>
    </Modal.body>
    <Modal.footer>
        <Button
            @type="primary"
            @label={{t "common.done"}}
            @wide={{true}}
            @action={{this.done}}
        />
    </Modal.footer>
</ModalDialog>
